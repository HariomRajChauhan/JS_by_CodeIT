// Define the person object
const person = {
    name: 'HARIOM Raj Chauhan',
    age: 19,
    city: 'Dharan',
};

// Use object destructuring to extract the properties
const { name, age, city } = person;

// Print the extracted properties
console.log(`Name: ${name}`);
console.log(`Age: ${age}`);
console.log(`City: ${city}`);