// Create an array colors containing the elements "red", "green", and "blue". Use array destructuring to assign the first two elements to variables firstColor and secondColor, and print these variables


const colors = ["red", "green", "blue"];
const [firstColor, secondColor] = colors;
console.log(firstColor);
console.log(secondColor);